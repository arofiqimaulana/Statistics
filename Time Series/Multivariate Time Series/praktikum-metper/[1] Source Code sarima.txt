*import data;
proc import out=work.latihan datafile = 'D:\KULIAH\SEMESTER 8\ASISTEN METPER\MATERI 1\Data Latihan.xlsx'
dbms=xlsx replace;
sheet='sarima';
getnames=yes;
run;
*print data;
proc print data=latihan;
run;
*manipulasi data;
data latihan;
set latihan;
z=0;
ty=milk**0.75;
run;
*transformasi Box Cox;
proc transreg maxiter=0 nozeroconstant;
      model BoxCox(milk) = identity(z);
      output;
run;
*arima;
proc arima data=latihan;
identify var=ty(1,12) nlag=25 stationarity=(adf);
run;
estimate p=0 q=(1)(12)noconstant method=ML;
run;
forecast out=forecasting lead=12;
run;
proc univariate normaltest;
var residual;
run;
proc print data=forecasting;
run;
data forecasting;
set forecasting;
ramalan=exp((1/0.75)*log(forecast));
milk=exp((1/0.75)*log(ty));
run;

